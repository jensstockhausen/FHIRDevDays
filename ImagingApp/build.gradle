group 'org.fhir'
version '1.0-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'application'

mainClassName = "org.fhir.app.ImagingApplication"

dependencies {
    compile project(":ImagingLib")

    compile lib.hapi

    compile lib.commons
    compile lib.log

    testCompile lib.junit
}
